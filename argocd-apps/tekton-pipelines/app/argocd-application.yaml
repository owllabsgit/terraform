apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/louayparadigma/test1-owl.git
    targetRevision: HEAD
    path: argocd-apps/tekton-pipelines/app   # <- This MUST match Git exactly
  destination:
    server: https://kubernetes.default.svc
    namespace: test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

