# 1. ClusterRole to allow managing EndpointSlices
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: el-endpointslice-role
rules:
  - apiGroups: [""]
    resources: ["endpointslices"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]
